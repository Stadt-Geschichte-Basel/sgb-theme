title: sgb-theme
author: Stadt.Geschichte.Basel
version: 1.0.0
quarto-required: '>=1.6.0'
contributes:
  project:
    metadata:
      type: website
  website:
    metadata:
      # Branding
      favicon: favicon.png
      logo: assets/img/logo.svg

      # SEO and Social Cards
      open-graph: true
      twitter-card: true

      # Navigation and UI
      repo-actions: [edit, issue]
      page-navigation: true
      bread-crumbs: true
      back-to-top-navigation: true

      navbar:
        logo: assets/img/logo.svg
        logo-alt: ''
        tool-collapse: true

      # Search configuration
      search:
        show-item-context: true
        type: overlay

  # Default format configurations
  formats:
    html:
      # Theme and appearance
      theme:
        - cosmo
        - _theme.scss
      css: styles.css
      mainfont: Euclid Circular B
      # Features
      toc: true
      link-external-newwindow: true
      code-copy: true
      code-overflow: wrap
      title-block-categories: false

      # Analytics - remember to replace the domain
      include-in-header:
        text: |
          <script defer data-domain="stadtgeschichtebasel.ch" src="https://plausible.io/js/script.outbound-links.js"></script>
